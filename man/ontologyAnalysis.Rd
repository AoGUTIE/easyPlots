% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ontoAnalysis.R
\name{ontologyAnalysis}
\alias{ontologyAnalysis}
\title{Perform Gene Ontology (GO) Enrichment Analysis}
\usage{
ontologyAnalysis(
  data,
  codeType,
  DEG,
  Output_Language,
  Input_Language,
  Translate = TRUE,
  FC,
  PV,
  maxSize,
  Filtering = TRUE,
  GeneList
)
}
\arguments{
\item{data}{A data frame containing at least the columns: \code{log2FoldChange}, \code{pvalue}, \code{SYMBOL} or \code{ENSEMBL}, \code{geneID}, \code{ONTOLOGY}, and \code{Description}.}

\item{codeType}{A character string specifying the gene identifier type to use in the enrichment analysis: \code{"SYMBOL"} or \code{"ENSEMBL"}.}

\item{DEG}{A character string indicating which DEGs to analyze: \code{"up"} for upregulated, \code{"down"} for downregulated.}

\item{Output_Language}{A character string with the language code (e.g., \code{"es"}) to which GO term descriptions should be translated.}

\item{Input_Language}{A character string with the language code (e.g., \code{"en"}) of the input GO term descriptions.}

\item{Translate}{Logical (\code{TRUE} or \code{FALSE}); whether to translate the GO term descriptions from \code{Input_Language} to \code{Output_Language}.}

\item{FC}{Numeric; the absolute log2 fold-change threshold used to define upregulated and downregulated genes.}

\item{PV}{Numeric; the p-value threshold used to define significantly differentially expressed genes.}

\item{maxSize}{Integer; maximum number of GO terms to retain for each ontology category (BP, MF, CC).}

\item{Filtering}{Logical (\code{TRUE} or \code{FALSE}); whether to filter the GO results to include only terms associated with genes in \code{GeneList}.}

\item{GeneList}{A character vector of gene identifiers to filter the GO results against (only used if \code{Filtering = TRUE}).}
}
\value{
A named list of three data frames:
\describe{
\item{BiologicalProcess}{Data frame of enriched Biological Processes (BP).}
\item{MolecularFunctions}{Data frame of enriched Molecular Functions (MF).}
\item{CellularComponents}{Data frame of enriched Cellular Components (CC).}
}
}
\description{
This function performs a Gene Ontology (GO) enrichment analysis on a set of differentially expressed genes (DEGs),
using the \code{clusterProfiler} package. It can optionally translate the resulting GO term descriptions and/or filter
the results to include only those associated with a specified gene list.
}
\details{
The function first filters the input \code{data} by the specified \code{FC} and \code{PV} thresholds to select significant DEGs.
Depending on the \code{DEG} parameter, it runs the GO enrichment analysis using the upregulated or downregulated gene set.
Optionally, it can translate GO term descriptions and/or filter the terms based on a user-provided \code{GeneList}.
The output consists of the top \code{maxSize} GO terms (sorted by \code{Count}) for each of the three ontology categories.
}
\examples{
\dontrun{
result <- ontologyAnalysis(
  data = my_data,
  codeType = "SYMBOL",
  DEG = "up",
  Output_Language = "es",
  Input_Language = "en",
  Translate = TRUE,
  FC = 1,
  PV = 0.05,
  maxSize = 10,
  Filtering = TRUE,
  GeneList = c("TP53", "BRCA1", "EGFR")
)

result$BiologicalProcess
}

}
