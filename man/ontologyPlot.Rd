% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ontoPlot.R
\name{ontologyPlot}
\alias{ontologyPlot}
\title{Create and Arrange Ontology Enrichment Barplots}
\usage{
ontologyPlot(
  data,
  fontSize,
  margin_left,
  margin_right,
  margin_top,
  margin_bottom,
  color
)
}
\arguments{
\item{data}{A list of data frames. Each data frame must include the columns: \code{Position}, \code{Count}, \code{pvalue}, and \code{title}.}

\item{fontSize}{Numeric; font size for the y-axis text labels.}

\item{margin_left}{Numeric vector; left margins for each plot.}

\item{margin_right}{Numeric vector; right margins for each plot.}

\item{margin_top}{Numeric vector; top margins for each plot.}

\item{margin_bottom}{Numeric vector; bottom margins for each plot.}

\item{color}{Character vector of length 2; colors defining the gradient for the \code{pvalue} fill (e.g., \code{c("blue", "red")}).}
}
\value{
A single ggpubr object combining the individual barplots arranged in one column.
}
\description{
This function generates barplots from a list of data frames containing ontology enrichment results.
Each data frame is plotted with customized font size, margins, and a color gradient. The plots are combined
vertically into a single figure.
}
\details{
The function iterates through each data frame in \code{data}, generating a horizontal barplot for each one using \code{ggplot2},
and adjusting the margins individually according to the corresponding values in \code{margin_left}, \code{margin_right},
\code{margin_top}, and \code{margin_bottom}. The plots are then combined vertically using \code{ggpubr::ggarrange()}.
}
\examples{
\dontrun{
final_plot <- ontologyPlot(
  data = list(df1, df2, df3),
  fontSize = 12,
  margin_left = c(5, 5, 5),
  margin_right = c(5, 5, 5),
  margin_top = c(5, 5, 5),
  margin_bottom = c(5, 5, 5),
  color = c("blue", "red")
)

print(final_plot)
}

}
