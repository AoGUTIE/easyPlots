% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot.R
\name{volcanoPlot}
\alias{volcanoPlot}
\title{Generate a Volcano Plot for Differential Expression Results}
\usage{
volcanoPlot(
  data,
  FC,
  statSig,
  PV,
  STAT,
  Description = TRUE,
  Filtering,
  maxGenes,
  TopGenes = TRUE,
  List = TRUE,
  GeneList,
  colors1,
  colors2,
  colors3,
  Ymax,
  Xmax,
  Ymin,
  Xmin
)
}
\arguments{
\item{data}{A data frame containing at least \code{log2FoldChange}, \code{pvalue}, \code{stat}, \code{Description}, and \code{SYMBOL} columns.}

\item{FC}{Numeric; log2 fold-change threshold to define up- and down-regulated genes.}

\item{statSig}{Character; which statistic to use: \code{"t-test"} or \code{"p-value"}.}

\item{PV}{Numeric; p-value threshold to define significance (used if \code{statSig = "p-value"}).}

\item{STAT}{Numeric; statistic threshold to define significance (used if \code{statSig = "t-test"}).}

\item{Description}{Logical (\code{TRUE} or \code{FALSE}); whether to filter rows based on the \code{Description} column.}

\item{Filtering}{Character; value in the \code{Description} column to filter by if \code{Description = TRUE}.}

\item{maxGenes}{Integer; maximum number of top up- and down-regulated genes to label.}

\item{TopGenes}{Logical (\code{TRUE} or \code{FALSE}); whether to highlight the top \code{maxGenes} genes based on significance.}

\item{List}{Logical (\code{TRUE} or \code{FALSE}); whether to highlight a specific list of genes.}

\item{GeneList}{Character vector; list of gene symbols to highlight if \code{List = TRUE}.}

\item{colors1}{Character vector of length 3; colors for \code{"Up Regulated"}, \code{"Down Regulated"}, and \code{"Not Significant"}.}

\item{colors2}{Character; color used for axes, lines, and top gene labels.}

\item{colors3}{Character; color used for the user-specified gene list.}

\item{Ymax, Ymin}{Numeric; y-axis limits.}

\item{Xmax, Xmin}{Numeric; x-axis limits.}
}
\value{
A \code{ggplot2} object representing the volcano plot.
}
\description{
This function generates a customizable volcano plot from differential expression analysis results,
highlighting significant genes based on fold-change and statistical criteria, and optionally labeling
top or user-specified genes.
}
\details{
The function classifies genes into \code{"Up Regulated"}, \code{"Down Regulated"}, or \code{"Not Significant"} based on \code{FC}, \code{PV}, and/or \code{STAT}.
Top significant genes and/or user-specified genes (\code{GeneList}) can be highlighted and labeled.
Two modes of statistical significance are supported: \code{"t-test"} (using \code{STAT}) and \code{"p-value"} (using \code{PV}).
}
\examples{
\dontrun{
plot <- volcanoPlot(
  data = my_data,
  FC = 1,
  statSig = "p-value",
  PV = 0.05,
  STAT = 2,
  Description = TRUE,
  Filtering = "some category",
  maxGenes = 10,
  TopGenes = TRUE,
  List = TRUE,
  GeneList = c("TP53", "BRCA1", "EGFR"),
  colors1 = c("red", "blue", "gray"),
  colors2 = "black",
  colors3 = "green",
  Ymax = 10,
  Ymin = 0,
  Xmax = 5,
  Xmin = -5
)
print(plot)
}

}
